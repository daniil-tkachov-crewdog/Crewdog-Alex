# Supabase — public client credentials (safe to expose to the browser).
# Real values live in the Render dashboard; this file only documents the names.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
