# Feed-URL on-ramp (deferred)

Polls a board's feed URL on a schedule and normalizes rows into the internal
job schema. Runs as its own component on Render Cron (see ../../workers).
Marked "coming soon" in the Import tab. Not built in v1.
